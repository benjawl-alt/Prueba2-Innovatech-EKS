version: '3.8'

services:
  app-frontend:
    build:
      context: ./front_despacho
      dockerfile: Dockerfile
      args:
        - VITE_API_URL_VENTAS=http://3.226.238.81
        - VITE_API_URL_DESPACHOS=http://3.226.238.81
    container_name: app-front-ventas
    ports:
      - "80:80"
    restart: always