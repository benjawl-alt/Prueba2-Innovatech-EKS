version: '3.8'

services:
  app-frontend:
    build:
      context: ./front_despacho
      dockerfile: Dockerfile
      args:
        - VITE_API_URL_VENTAS=http://100.25.248.207
        - VITE_API_URL_DESPACHOS=http://100.25.248.207
    container_name: app-front-ventas
    ports:
      - "80:80"
    restart: always