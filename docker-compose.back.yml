version: '3.8'

services:
  back-ventas:
    build: ./back-Ventas_SpringBoot/Springboot-API-REST
    ports:
      - "8081:8080"
    environment:
      # Conectamos directo a la EC2 DATA usando su IP Privada interna
      - DB_ENDPOINT=10.0.3.45
      - DB_PORT=27017
      - DB_NAME=innovatech_db

  back-despacho:
    build: ./back-Despachos_SpringBoot/Springboot-API-REST-DESPACHO
    ports:
      - "8082:8080"
    environment:
      # Conectamos directo a la EC2 DATA usando su IP Privada interna
      - DB_ENDPOINT=10.0.3.45
      - DB_PORT=27017
      - DB_NAME=innovatech_db


      #ldlll#